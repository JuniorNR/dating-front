import { FC } from 'react';
import { Announcements } from '@/widgets';

const AnnouncementsPage: FC = () => {
	return (
		<div>
			<Announcements />
		</div>
	);
};

export default AnnouncementsPage;
